setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    S3_BUCKET: builders-database
build:
  docker:
    web: Dockerfile
run:
  web: /usr/bin/java -jar customers.jar -Dspring.profiles.active=prod